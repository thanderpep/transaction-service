spring:
  datasource:
    url: jdbc:h2:mem:cajudb;MODE=MYSQL
    username: caju
    password: caju123
    baseline-on-migrate: true
    driverClassName: org.h2.Driver
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
